"""
Define a function that when passed a number determines if that number is prime.
Remember that a prime number is a natural number greater than one that has no positive divisiors other tah one and itself.

Test code:
print(is_prime(3))
print(is_prime(4))
print(is_prime(7))
print(is_prime(23))
print(is_prime(42))
"""

# Well, we can't use loops so my only other thought is recursion.

def is_prime(n, total=0):

    pass

    # NOT COMPLETE
